 CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
PROJECT(SeriousSam)

FILE(GLOB_RECURSE SERIOUSSAM_INCLUDES "*.h")
FILE(GLOB_RECURSE SERIOUSSAM_SOURCES "*.cpp")

INCLUDE_DIRECTORIES ("${CMAKE_SOURCE_DIR}")
ADD_EXECUTABLE (SeriousSam ${SERIOUSSAM_SOURCES} ${SERIOUSSAM_INCLUDES})
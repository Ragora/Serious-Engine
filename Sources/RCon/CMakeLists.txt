CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
PROJECT(RCon)

FILE(GLOB_RECURSE RCON_INCLUDES "*.h")
FILE(GLOB_RECURSE RCON_SOURCES "*.cpp")

INCLUDE_DIRECTORIES ("../")
ADD_EXECUTABLE (RCon ${RCON_SOURCES} ${RCON_INCLUDES})
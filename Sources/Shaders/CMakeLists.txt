CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
PROJECT(Shaders)

FILE(GLOB_RECURSE SHADERS_INCLUDES "*.h")
FILE(GLOB_RECURSE SHADERS_SOURCES "*.cpp")

INCLUDE_DIRECTORIES ("${CMAKE_SOURCE_DIR}")
ADD_EXECUTABLE (Shaders ${SHADERS_SOURCES} ${SHADERS_INCLUDES})
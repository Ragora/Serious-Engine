 
CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
PROJECT(MakeFONT)

FILE(GLOB_RECURSE MAKEFONT_INCLUDES "*.h")
FILE(GLOB_RECURSE MAKEFONT_SOURCES "*.cpp")

INCLUDE_DIRECTORIES ("../")
ADD_EXECUTABLE (MakeFONT ${MAKEFONT_SOURCES} ${MAKEFONT_INCLUDES})
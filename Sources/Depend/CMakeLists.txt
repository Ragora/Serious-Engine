CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
PROJECT(Depend)

FILE(GLOB_RECURSE DEPEND_INCLUDES "*.h")
FILE(GLOB_RECURSE DEPEND_SOURCES "*.cpp")

INCLUDE_DIRECTORIES ("${CMAKE_SOURCE_DIR}")
ADD_EXECUTABLE (Depend ${DEPEND_SOURCES} ${DEPEND_INCLUDES})

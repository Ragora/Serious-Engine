CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
PROJECT(GameGUIMP)

FILE(GLOB_RECURSE GAMEGUIMP_INCLUDES "*.h")
FILE(GLOB_RECURSE GAMEGUIMP_SOURCES "*.cpp")

INCLUDE_DIRECTORIES ("../")
ADD_EXECUTABLE (GameGUIMP ${GAMEGUIMP_SOURCES} ${GAMEGUIMP_INCLUDES})
CMAKE_MINIMUM_REQUIRED (VERSION 2.6)
PROJECT (GameMP)

FILE(GLOB_RECURSE GAMEMP_INCLUDES "*.h")
FILE(GLOB_RECURSE GAMEMP_SOURCES "*.cpp")

INCLUDE_DIRECTORIES ("${CMAKE_CURRENT_SOURCE_DIR}" "${CMAKE_SOURCE_DIR}")
ADD_LIBRARY (GameMP "STATIC" ${GAMEMP_SOURCES} ${GAMEMP_INCLUDES})
